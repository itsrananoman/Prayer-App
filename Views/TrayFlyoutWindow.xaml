<Window x:Class="Prayer.Views.TrayFlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Prayer Tray Menu"
        Width="260" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="{StaticResource MainFontFamily}">

    <Border Background="{StaticResource SurfaceCardBrush}"
            BorderBrush="{StaticResource BorderGoldBrush}"
            BorderThickness="1.5"
            CornerRadius="12"
            Padding="8,10"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Direction="270" Opacity="0.55" Color="#000000"/>
        </Border.Effect>

        <StackPanel>
            <!-- TRAY FLYOUT HEADER -->
            <Border Background="#061A14" CornerRadius="8" Padding="10,8" Margin="0,0,0,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="pack://application:,,,/Resources/Icons/Prayer.ico"
                           Width="22" Height="22"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="PRAYER" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}"/>
                            <TextBlock Text=" • " FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                            <TextBlock Text="صلوٰۃ" FontSize="12" FontWeight="Bold" FontFamily="{StaticResource ArabicFontFamily}" Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                        <TextBlock x:Name="NextPrayerInfoText" Text="Salah Focus Lock" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- MENU ITEMS -->
            <Button Style="{StaticResource TrayMenuItemButton}" Click="OnOpenDashboardClick">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🕌" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Open Dashboard" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource TrayMenuItemButton}" Click="OnOpenSettingsClick">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚙️" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Settings &amp; Preferences" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button x:Name="TestLockMenuItem" Style="{StaticResource TrayMenuItemButton}" Click="OnTestLockClick">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔒" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Test Lock Screen (10s)" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- DIVIDER -->
            <Rectangle Height="1" Fill="{StaticResource BorderSubtleBrush}" Margin="4,4,4,4"/>

            <Button Style="{StaticResource TrayMenuItemButton}" Click="OnExitClick">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="❌" FontSize="13" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Exit Application" Foreground="#E57373" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</Window>
