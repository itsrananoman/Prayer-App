<UserControl x:Class="Prayer.Views.Pages.PrayerTimesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             FontFamily="{StaticResource MainFontFamily}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,8,16">

            <!-- Title & Calculation Info Card -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16" Padding="18,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Data="{StaticResource GeoPrayerTimes}" Stroke="{StaticResource TextGoldBrush}" StrokeThickness="1.5"
                                  Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Daily Salah Schedule" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding LocationDisplay, StringFormat='📍 {0}'}" FontSize="13" Foreground="{StaticResource TextGoldBrush}" Margin="0,0,0,2"/>
                        <TextBlock Text="{Binding CalculationMethodName, StringFormat='Calculation Method: {0}'}" FontSize="11.5" Foreground="{StaticResource TextMutedBrush}"/>
                    </StackPanel>

                    <!-- Sunrise / Sunset Pill Cards -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#092019" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="10" Padding="14,8" Margin="0,0,10,0">
                            <StackPanel HorizontalAlignment="Center">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,2">
                                    <Path Data="{StaticResource GeoFajr}" Stroke="{StaticResource TextGoldBrush}" StrokeThickness="1.4" Width="12" Height="12" Stretch="Uniform" Margin="0,0,4,0"/>
                                    <TextBlock Text="Sunrise" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding SunriseTime}" FontSize="13.5" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#092019" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="10" Padding="14,8">
                            <StackPanel HorizontalAlignment="Center">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,2">
                                    <Path Data="{StaticResource GeoMaghrib}" Stroke="{StaticResource TextGoldBrush}" StrokeThickness="1.4" Width="12" Height="12" Stretch="Uniform" Margin="0,0,4,0"/>
                                    <TextBlock Text="Sunset" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding SunsetTime}" FontSize="13.5" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Detailed Prayer Cards -->
            <ItemsControl ItemsSource="{Binding PrayerCards}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ModernCard}" Margin="0,0,0,10" Padding="18,14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <Border Width="40" Height="40" CornerRadius="20" Background="#0C2820" Margin="0,0,14,0" VerticalAlignment="Center">
                                    <Path Width="20" Height="20" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Path.Style>
                                            <Style TargetType="Path">
                                                <Setter Property="Stroke" Value="{StaticResource TextGoldBrush}"/>
                                                <Setter Property="StrokeThickness" Value="1.5"/>
                                                <Setter Property="Fill" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IconKind}" Value="Sunrise">
                                                        <Setter Property="Data" Value="{StaticResource GeoFajr}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IconKind}" Value="Mosque">
                                                        <Setter Property="Data" Value="{StaticResource GeoDhuhr}"/>
                                                        <Setter Property="Fill" Value="{StaticResource TextGoldBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IconKind}" Value="Sun">
                                                        <Setter Property="Data" Value="{StaticResource GeoAsr}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IconKind}" Value="Sunset">
                                                        <Setter Property="Data" Value="{StaticResource GeoMaghrib}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IconKind}" Value="Moonrise">
                                                        <Setter Property="Data" Value="{StaticResource GeoMaghrib}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IconKind}" Value="Moon">
                                                        <Setter Property="Data" Value="{StaticResource GeoIsha}"/>
                                                        <Setter Property="Fill" Value="{StaticResource TextGoldBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Path.Style>
                                    </Path>
                                </Border>

                                <!-- Prayer Names -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock Text="{Binding UrduName}" FontSize="13" FontFamily="{StaticResource ArabicFontFamily}" Foreground="{StaticResource TextGoldBrush}"/>
                                </StackPanel>

                                <!-- Formatted Time -->
                                <TextBlock Grid.Column="2" Text="{Binding FormattedTime}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>

                                <!-- Status Badge -->
                                <Border Grid.Column="4" Background="#092019" BorderBrush="{StaticResource BorderGoldBrush}" BorderThickness="1" CornerRadius="6" Padding="10,4" VerticalAlignment="Center">
                                    <TextBlock Text="Automatic Focus Lock Active" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMintBrush}"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
