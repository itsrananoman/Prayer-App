<UserControl x:Class="Prayer.Views.Pages.AboutPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             FontFamily="{StaticResource MainFontFamily}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,8,16">

            <!-- Hero Branding Card -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16" Padding="24,20" BorderBrush="{StaticResource BorderGoldBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Image Source="pack://application:,,,/Resources/Icons/Prayer.ico"
                           Width="68" Height="68"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           VerticalAlignment="Center" Margin="0,0,20,0"/>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding AppTitle}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}"/>
                        <TextBlock Text="{Binding AppTagline}" FontSize="14" FontStyle="Italic" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,6"/>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#10382E" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="6" Padding="8,3" Margin="0,0,8,0">
                                <TextBlock Text="{Binding Version}" FontSize="11.5" FontWeight="Bold" Foreground="{StaticResource TextMintBrush}"/>
                            </Border>
                            <Border Background="#10382E" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="6" Padding="8,3">
                                <TextBlock Text="{Binding Publisher}" FontSize="11.5" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}"/>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Mission Statement -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,14" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="🎯 Purpose &amp; Mission" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding MissionStatement}" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap" LineHeight="22"/>
                </StackPanel>
            </Border>

            <!-- Developer Information -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,14" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="👨‍💻 Developer &amp; Publishing" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Developer}" FontSize="13.5" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,2"/>
                    <TextBlock Text="{Binding Copyright}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- Verified Data Sources & Attributions -->
            <Border Style="{StaticResource ModernCard}" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="📜 Content Authenticity &amp; Attributions" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}" Margin="0,0,0,10"/>

                    <!-- Aladhan -->
                    <StackPanel Margin="0,0,0,8">
                        <TextBlock Text="• Prayer Times API" FontSize="12.5" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}"/>
                        <TextBlock Text="{Binding AladhanAttribution}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="10,2,0,0"/>
                    </StackPanel>

                    <!-- Quran Tanzil -->
                    <StackPanel Margin="0,0,0,8">
                        <TextBlock Text="• The Noble Qur'an Text &amp; Translations" FontSize="12.5" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}"/>
                        <TextBlock Text="{Binding QuranAttribution}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="10,2,0,0"/>
                    </StackPanel>

                    <!-- Hadith Bukhari & Muslim -->
                    <StackPanel>
                        <TextBlock Text="• Authentic Hadith Collections" FontSize="12.5" FontWeight="Bold" Foreground="{StaticResource TextGoldBrush}"/>
                        <TextBlock Text="{Binding HadithAttribution}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="10,2,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
